<div id="account-update-container">
  <div id="account-update-menu">
    <div class="account-menu-item account-menu-selected" id="account-edit">
      エディット
    </div>
    <div class="account-menu-item" id="account-delete">
      削除
    </div>
  </div>

  <div id="account-update-body">
    <div id="account-edit-body" class="account-body">

      <div class="account-update-title">
        <p class="colibri-devise-title">アカウント情報をエディットする</p>
      </div>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.label :name, 'ご自身のユーザー名', class: "colibri-form-label" %><br />
          <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "form-control"   %>
        </div>

        <div class="form-group">
          <%= f.label :kana, 'ユーザー名(フリガナ)', class: "colibri-form-label" %><br />
          <%= f.text_field :kana, autocomplete: "name", class: "form-control"   %>
        </div>

        <div class="form-group">
          <%= f.label :nurse_id, '従業員に紐づける', class: "colibri-form-label" %><br />
          <%= f.collection_select :nurse_id, current_user.corporation.nurses.displayable.order_by_kana, :id, :name, {prompt: '従業員を選択する'} %>
        </div>

        <div class="form-group">
          <%= f.label :nurse_id, 'シフトのデフォルトページ', class: "colibri-form-label" %><br />
          <%= f.select :schedule_default_url_option, options_for_select(user_default_url_options(resource), resource.schedule_default_url_option)  %>
        </div>

        <div class="form-group">
          <%= f.label :email, class: "colibri-form-label" %><br />
          <%= f.email_field :email, readonly: true, autocomplete: "email", class: "form-control"   %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>ただいまメールアドレスの確認済み: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div class="form-group">
          <%= f.label :password, '変更後パスワード(6桁以上)', class: "colibri-form-label" %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control"   %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation, '変更後パスワードの確認', class: "colibri-form-label" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control"   %>
        </div>

        <div class="form-group">
          <%= f.label :current_password, class: "colibri-form-label" %> <i>(アカウントをアップデートするためには現在のパスワードが必要です)</i><br />
          <%= f.password_field :current_password, autocomplete: "off", class: "form-control"   %>
        </div>

        <div class="actions">
          <%= f.submit "アップデートする" , class: 'btn btn-info btn-block form-control' %>
        </div>
      <% end %>
      
    </div>

    <div id="account-delete-body" style="display: none" class="account-body">

      <div class="account-update-title">
        <p class="colibri-devise-title">アカウントを削除する</p>
      </div>

      <p><%= button_to "アカウントを削除する", registration_path(resource_name), data: { confirm: "アカウントが削除されます" }, method: :delete, class: 'btn btn-danger btn-block form-control' %></p>
    </div>
  </div>
  
</div>

