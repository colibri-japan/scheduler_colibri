が<span class="activity-important-fields"><%= "#{activity.parameters[:patient_name]}様" %></span>の<%= toggle_cancelled_activity_text(activity.parameters[:previous_cancelled]) %><br/>
<%= activity.parameters[:nurse_name] %>
<%= activity.parameters[:starts_at].strftime("%-m月%-d日#{weekday(activity.parameters[:starts_at])} %-H:%M") if activity.parameters[:starts_at].present? %>
~ <%= activity.parameters[:ends_at].try(:strftime, "%-H:%M") %>