if (window.location.pathname.includes('posts')) {
  $('#post-<%= @post.id %> > .td-patient-tag').html("<%= j @post.patients.map(&:name).join('、') %>");
  $('#post-<%= @post.id %> > .td-post-body').html("<%= j simple_format(@post.body) %>");
  $('#post-<%= @post.id %> > .td-post-readers').html("<%= j @post_readers.join('、') %>");
  $('.modal').modal('hide');
  $('.modal-backdrop').remove();
  clickableTableRowPost()
} else {
  $('.modal').modal('hide');
  $('.modal-backdrop').remove();
  if ($('.activity_back_icon').length > 0) {
    $('.activity_back_icon').click()
  }
  $.getScript('/posts_widget.js');
  scrollPosts()
  clickablePost()
}