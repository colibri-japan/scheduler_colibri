$('.modal').modal('hide');
$('.calendar').fullCalendar(
  'renderEvent', 
  jQuery.parseJSON("<%= j render(partial: @appointment, formats: :json, locals: {appointment: @appointment}).html_safe %>"),
  true
);