が自社サービス以外の予定を追加しました<br/>
<span class="activity-important-fields"><%= "#{activity.nurse_name}" %><% "#{activity.patient_name}様" if activity.patient_name.present? %></span>
<% if activity.trackable.present? %>
	<%= activity.trackable.starts_at.strftime("%-m月%-d日#{weekday(activity.trackable.try(:starts_at))} %-H:%M") %> ~ <%= weekday(activity.trackable.ends_at) %><%= activity.trackable.ends_at.strftime("%-H:%M") %>
<% end %>