<% if activity.trackable.present? %>
	が<span class="activity-important-fields"><%= activity.trackable.nurse.try(:name) %></span>のシフト希望を追加しました<br/>
	<%= activity.trackable.starts_at.strftime("%-m月%-d日以降 #{weekday(activity.trackable.anchor)} %-H:%M") %> ~ <%= activity.trackable.ends_at.strftime("%-H:%M") %>
<% else %>
	がシフトの希望日を削除しました。
<% end %>