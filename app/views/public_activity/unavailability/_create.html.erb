<% if activity.trackable %>
	がサービスが不可能な時間を追加しました。<br/>
	<span class="activity-important-fields"><%= activity.trackable.nurse.present? ? "#{activity.trackable.nurse.try(:name)   }" : "#{activity.trackable.patient.try(:name)}様"  %></span>
	<%= weekday(activity.trackable.start) %><%= activity.trackable.start.strftime("%H:%M") %> ~ <%= weekday(activity.trackable.end) %><%= activity.trackable.end.strftime("%H:%M") %>
<% else %>
	がその後削除されたサービスが不可能な時間を追加しました。
<% end %>