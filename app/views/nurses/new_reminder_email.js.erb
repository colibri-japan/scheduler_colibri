$('.modal').modal('hide');
$('.modal-backdrop').remove();
$('#colibri-batch-action-button').click()

<% if @nurse.phone_mail.present? %>
    $('#remote_container').html('<%= j render "new_reminder_email" %>');
    $('#reminder-email-modal').modal('show');
    sendReminder()
    selectizeCustomEmailDays()
<% else %>
    alert('従業員のメールアドレスを登録してください。')
<% end %>