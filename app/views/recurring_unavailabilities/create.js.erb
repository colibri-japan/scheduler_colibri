$('.modal').modal('hide');
$('.nurse_calendar').fullCalendar(
  'renderEvents', 
  jQuery.parseJSON("<%= j render(partial: 'recurring_unavailabilities/recurring_unavailability.json', locals: {recurring_unavailability: @recurring_unavailability}).html_safe %>"),
  false
);