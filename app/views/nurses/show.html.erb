<%= javascript_tag do %>
	window.appointmentsURL = "<%= j planning_appointments_url(@planning, params: {nurse_id: @nurse.id}) %>";
	window.recurringAppointmentsURL = "<%= j planning_recurring_appointments_url(@planning, params: {nurse_id: @nurse.id}) %>";
	window.corporationNursesURL = "<%= j corporation_nurses_url(@corporation, format: :json) %>";
	window.createRecurringUnavailabilityURL = "<%= j new_planning_recurring_unavailability_url(@planning) %>";
	window.unavailabilitiesURL = "<%= j planning_unavailabilities_url(@planning, params: {nurse_id: @nurse.id}) %>";
	window.recurringUnavailabilitiesURL = "<%= j planning_recurring_unavailabilities_url(@planning, params: {nurse_id: @nurse.id}) %>";
	window.validRangeStart = "<%= j @start_valid %>";
	window.validRangeEnd = "<%= j @end_valid %>";
	window.nurseId = "<%= j @nurse.id.to_s %>"
<% end %>

<%= render 'layouts/planning_menu' %>
<%= render 'plannings/planning_nurses_submenu' %>

<h2><%= @nurse.name %>の空き時間を変換する</h2>

<div class="nurse_calendar">
  
</div>

<%= link_to 'Edit', edit_planning_path(@planning) %> |
<%= link_to 'Back', plannings_path %>
