<%= @nurse.name %>様、<%= @corporation.name %>のサービスのスケジュールです。

<%= @custom_message %>

<% @appointments.each do |appointment| %>
	<%= appointment.patient.try(:name) %>様: <%= appointment.start.strftime("%-d日")  %><%= weekday(appointment.start) %> <%= appointment.start.strftime("%-H:%M") %>~<%= appointment.end.strftime("%-H:%M") %>
	<% if appointment.description.present? %>
		コメント：<%= appointment.description %>
	<% end %>
<% end %>

以上、宜しくお願い致します。