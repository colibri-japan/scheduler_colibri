<%= form_with model: provided_service, url: [@nurse, provided_service] do |form| %>
  <% if provided_service.errors.any? %>
  <div id="error_explanation">
    <h2>エラーが発生しました：</h2>

    <ul>
      <% provided_service.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>


  <div class="modal-body">

      <div class="form-group">
        <%= form.label :title, "タイトル", class: "colibri-form-label-dark" %>
        <%= form.text_field :title, id: :provided_service_title, required: true, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :service_date, "日付", class: "colibri-form-label-dark" %>
        <%= form.date_select :service_date, id: :provided_service_service_date, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :total_wage, "合計額", class: "colibri-form-label-dark" %>
        <%= form.number_field :total_wage, {step: 1, id: :provided_service_total_wage, class: "form-control"} %>
      </div>

  </div>

  <div class="modal-footer">
    <div class="actions">
      <% if provided_service.new_record? %>
        <div class="btn btn-colibri-light" data-dismiss="modal">閉じる</div>
      <% end %>
      <%= link_to '削除', provided_service, method: :delete, data: {confirm: 'この手当が削除されます。'}, class: 'btn btn-danger' unless provided_service.new_record? %>
      <%= form.submit 'セーブする', class: 'btn btn-info' %>
    </div>
  </div>


<% end %>
