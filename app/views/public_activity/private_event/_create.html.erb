が自社サービス以外の予定を追加しました<br/>
<span class="activity-important-fields"><%= activity.parameters[:title] %></span><br/>
<span class="activity-important-fields"><%= "#{activity.parameters[:nurse_name]}" %><% "#{activity.parameters[:patient_name]}様" if activity.parameters[:patient_name].present? %></span>
<%= activity.parameters[:starts_at].strftime("%-m月%-d日#{weekday(activity.parameters[:starts_at])} %-H:%M") if activity.parameters[:starts_at].present? %>
~ <%= activity.parameters[:ends_at].strftime("%-m月%-d日#{weekday(activity.parameters[:ends_at])} %-H:%M") if activity.parameters[:ends_at].present? %>